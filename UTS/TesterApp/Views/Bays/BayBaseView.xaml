<UserControl x:Class="TesterApp.Views.Bays.BayBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Background="Green" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
    <GroupBox Name="BayGroupBox" Background="Green" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <GroupBox.Header>
            <Label Name="BayTag" Foreground="White" FontSize="15" />
        </GroupBox.Header>
        <DockPanel Name="BasePanel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <Grid DockPanel.Dock="Right" Name="StatusUpdateViewGrid" Margin="5" VerticalAlignment="Bottom"/>
            <Grid Name="MainGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </DockPanel>


    </GroupBox>
</UserControl>
