@model IEnumerable<BSDSPortal.Models.Bay>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@section menu{
    @Html.Partial("MenuView")
    }

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tester.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tester.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BayID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BayID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BayID })
        </td>
    </tr>
}

</table>
